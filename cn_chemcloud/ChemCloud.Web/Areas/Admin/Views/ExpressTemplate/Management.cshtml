@model IEnumerable<ChemCloud.Core.Plugins.IExpress>
<div>
    <div class="container">
        <!--<ul class="nav nav-tabs-custom clearfix">
            <strong>快递单模板</strong>
            <em class="primary-btn">
                <span class="glyphicon glyphicon-question-sign"></span>
                <div class="primary">
                    <h3>温馨提示：</h3>
                    <span>1、</span><p>编辑平台支持的快递模板</p>
                    <span>2、</span><p>目前支持10余种使用最多的快递</p>
                </div>
            </em>
        </ul>-->
        <table class="table">
            <thead>
                <tr>
                    <th>快递公司</th>
                    <th class="td-operate">操作</th>
                </tr>
            </thead>
            <tbody>
               @foreach (var template in Model)
               {
                <tr>
                    <td>@template.DisplayName</td>
                    <td class="td-operate"><span class="btn-a"><a href="Edit?name=@(template.Name)">编辑</a></span></td>
                </tr>
               }
            </tbody>
        </table>
    </div>

</div>

